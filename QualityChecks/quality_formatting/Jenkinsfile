pipeline {
    agent any

    stages {
        stage('EOL') {
            steps {
                sh """#!/bin/bash -xe
                    echo "hello From Formatting Check"
                    sleep 2
                    """
            }
        }
    }
}
